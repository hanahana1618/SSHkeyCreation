#!/bin/bash

#bash script to create a new ssh-keygen key

#ask user for 
#github email address
#install clip: sudo apt-get install geomview
#echo 

#check for existing SSH keys
ls -al ~/.ssh
#echo $existingkeys
#ask the user for input here to continue or not

#generate new ssh-keys
#ssh-keygen -t rsa -b 4096 -C "email"
